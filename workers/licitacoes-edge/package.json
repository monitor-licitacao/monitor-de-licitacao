{
  "name": "licitacoes-edge",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "dev": "wrangler dev --config wrangler.jsonc",
    "dry-run": "wrangler deploy --dry-run --config wrangler.jsonc",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "4.130.0"
  }
}
